#include <iostream>
#include <string>
#include <array>

int main(int argc, char* argv[]) {
    if(argc < 2)
        std::cout << "Incorrect number of parameters!\n";
    else 
    {
        
    }
  return(0);
}
